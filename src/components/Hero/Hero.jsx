import React from 'react';

const styles = {
  height: '100vh',
  width: '100vw',
  background: 'linear-gradient(to top, #001133, #3b5885)',
};

const Hero = () => {
  return <div style={styles}>Hero</div>;
};

export default Hero;
